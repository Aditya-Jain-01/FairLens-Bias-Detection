# FairLens — Unified Backend Dependencies
# Install with: pip install -r requirements.txt

# ── Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.6
python-dotenv>=1.0.1
pydantic>=2.7.1
slowapi>=0.1.9

# ── Data tools
pandas>=2.2.2
numpy>=1.26.4
joblib
scikit-learn>=1.5.0

# ── Fairness libraries
aif360>=0.6.1
fairlearn>=0.10.0

# ── SHAP
shap>=0.45.1

# ── PDF generation
weasyprint>=62.3
Jinja2>=3.1.3

# ── GCP storage (required when USE_LOCAL_STORAGE=false)
google-cloud-storage>=2.17.0

# ── Gemini — FairLens uses the Google AI Studio REST API directly (urllib).
# Do NOT add google-cloud-aiplatform or vertexai here — those enable Vertex AI
# billing on the GCP project. The GEMINI_API_KEY env var is sufficient.
# See: backend/services/gemini.py → _BASE = generativelanguage.googleapis.com

# ── PDF fallback (used in pdf_generator.py when WeasyPrint unavailable)
reportlab>=4.0.0

# ── ONNX (optional — only needed for .onnx model inference)
# onnxruntime

# ── Phase 2: Scalability
google-cloud-tasks>=2.16.0       # 2.1 Async job queue
google-cloud-firestore>=2.16.0   # 2.2 Job metadata database
redis[asyncio]>=5.0.0            # 2.3 Result caching (Cloud Memorystore)
cachetools>=5.3.0                # 2.3 In-memory fallback cache (no Redis needed locally)
