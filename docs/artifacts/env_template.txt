# FairLens — Local development environment
# Copy this to .env.local and fill in real values.
# NEVER commit .env.local to git.

# ── GCP (needed only on integration day — leave blank for local dev)
GCP_PROJECT_ID=fairlens-hackathon
GCP_REGION=us-central1
GCS_UPLOAD_BUCKET=fairlens-uploads
GCS_RESULTS_BUCKET=fairlens-results

# ── Gemini (Person 3 sets this up)
VERTEX_AI_LOCATION=us-central1
GEMINI_MODEL=gemini-1.5-pro

# ── App URLs
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8000
